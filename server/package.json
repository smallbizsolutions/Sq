{
  "name": "square-kiosk-server",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18.17" },
  "scripts": {
    "dev": "node server.js",
    "postinstall": "npm --prefix ../client ci || npm --prefix ../client install && npm --prefix ../client run build",
    "start": "NODE_ENV=production node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "square": "^44.0.0"
  }
}
