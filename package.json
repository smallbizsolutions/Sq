{
  "name": "vapi-square-order-backend",
  "version": "1.0.0",
  "description": "Minimal backend for VAPI to read Square catalog and create orders (Sandbox or Production via env).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "square": "^36.1.0"
  }
}
